{% extends 'layout.twig' %}

{% block content %}
	{% if page.gallery.fangate_image is defined %}
	<img src="{{ page.gallery.fangate_image|raw }}">
	{% else %}
  	<h2>{{ app.translator.trans('gallery.fangate.h2') }}</h2>
  	<fb:like href="{{ facebook_apps.default.page_url }}" send="false" width="450" show_faces="true"></fb:like>
  	{% endif %}

{% endblock %}

{% block navigation %}
{% endblock %}
